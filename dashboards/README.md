# Executive dashboard

Open `index.html` in any modern browser. The dashboard is a standalone portfolio view built with HTML, CSS, and Chart.js.

The displayed values come from versioned files in `../outputs/`. When source data changes, run `python3 run_all.py`, review the regenerated outputs, and update dashboard constants only after validation.

The dashboard deliberately labels attribution as model sensitivity rather than causal incrementality.

